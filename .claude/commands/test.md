Run the Spendly test suite and report results.

Steps:
1. Run `venv/bin/pytest -v` from the project root.
2. Report how many tests passed, failed, or errored.
3. If any tests failed, show the failure output and suggest what to investigate.
